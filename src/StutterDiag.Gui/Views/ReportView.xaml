<UserControl x:Class="StutterDiag.Gui.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:StutterDiag.Gui.Models"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">

    <DockPanel Margin="20">
        <TextBlock DockPanel.Dock="Top" Text="Report" Style="{StaticResource H1}" />

        <TextBlock DockPanel.Dock="Top" Text="{Binding ServiceHint}" Style="{StaticResource Muted}"
                   Margin="0,0,0,10" TextWrapping="Wrap"
                   Visibility="{Binding ServiceHint, Converter={StaticResource NullOrEmptyToVis}}" />

        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,12">
            <StackPanel>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Format" VerticalAlignment="Center"
                               Style="{StaticResource Muted}" Margin="0,0,6,0" />
                    <ComboBox Grid.Column="1" ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat}" />

                    <TextBlock Grid.Column="2" Text="Output folder" VerticalAlignment="Center"
                               Style="{StaticResource Muted}" Margin="16,0,6,0" />
                    <TextBox Grid.Column="3" Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="4" Content="Browse…" Click="OnBrowseClick" Margin="8,0,0,0" />
                </Grid>

                <TextBlock Text="Redaction" Style="{StaticResource H2}" Margin="0,4,0,2" />
                <WrapPanel>
                    <CheckBox Content="Include process names" IsChecked="{Binding IncludeProcessNames}" Margin="0,0,18,0" />
                    <CheckBox Content="Include user names" IsChecked="{Binding IncludeUserNames}" Margin="0,0,18,0" />
                    <CheckBox Content="Include command lines" IsChecked="{Binding IncludeCommandLines}" Margin="0,0,18,0" />
                    <CheckBox Content="Include raw event XML" IsChecked="{Binding IncludeRawEventXml}" Margin="0,0,18,0" />
                    <CheckBox Content="Include full event log" IsChecked="{Binding IncludeFullEventLog}" />
                </WrapPanel>

                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="Generate" Command="{Binding GenerateCommand}" Style="{StaticResource AccentButton}"
                            Margin="0,0,8,0" />
                    <Button Content="Open" Command="{Binding OpenResultCommand}" Margin="0,0,8,0" />
                    <Button Content="Open folder" Command="{Binding OpenFolderCommand}" />
                </StackPanel>

                <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" Margin="0,8,0,0"
                           Visibility="{Binding StatusText, Converter={StaticResource NullOrEmptyToVis}}" />
            </StackPanel>
        </Border>

        <Grid DockPanel.Dock="Top" Margin="0,0,0,6">
            <TextBlock Text="Sessions" Style="{StaticResource H2}" Margin="0" />
            <Button Content="Reload sessions" Command="{Binding LoadSessionsCommand}" HorizontalAlignment="Right" />
        </Grid>

        <Border Style="{StaticResource Card}" Padding="4">
            <ListBox ItemsSource="{Binding Sessions}" BorderThickness="0" Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:SelectableSession}">
                        <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Display}" Margin="4,2" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </DockPanel>
</UserControl>
