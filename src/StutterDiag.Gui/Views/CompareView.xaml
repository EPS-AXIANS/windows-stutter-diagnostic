<UserControl x:Class="StutterDiag.Gui.Views.CompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <TextBlock Text="Compare" Style="{StaticResource H1}" />

            <TextBlock Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,0,0,10"
                       Text="Configuration A → measure, Configuration B → measure, compare." />

            <TextBlock Text="{Binding ServiceHint}" Style="{StaticResource Muted}" Margin="0,0,0,10"
                       TextWrapping="Wrap"
                       Visibility="{Binding ServiceHint, Converter={StaticResource NullOrEmptyToVis}}" />

            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <TextBlock Text="Sessions" Style="{StaticResource H2}" Margin="0" />
                        <Button Content="Reload sessions" Command="{Binding LoadSessionsCommand}"
                                HorizontalAlignment="Right" />
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Configuration A" Style="{StaticResource Muted}" />
                            <ComboBox ItemsSource="{Binding Sessions}" SelectedItem="{Binding SessionA}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <Run Text="#" /><Run Text="{Binding Id, Mode=OneWay}" />
                                            <Run Text="  " /><Run Text="{Binding StartedUtcIso, Mode=OneWay}" />
                                            <Run Text="  ·  " /><Run Text="{Binding Label, Mode=OneWay}" />
                                        </TextBlock>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Configuration B" Style="{StaticResource Muted}" />
                            <ComboBox ItemsSource="{Binding Sessions}" SelectedItem="{Binding SessionB}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <Run Text="#" /><Run Text="{Binding Id, Mode=OneWay}" />
                                            <Run Text="  " /><Run Text="{Binding StartedUtcIso, Mode=OneWay}" />
                                            <Run Text="  ·  " /><Run Text="{Binding Label, Mode=OneWay}" />
                                        </TextBlock>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="Generate comparison report" Command="{Binding CompareCommand}"
                                Style="{StaticResource AccentButton}" Margin="0,0,8,0" />
                        <Button Content="Open report" Command="{Binding OpenResultCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" Margin="0,0,0,8"
                       Visibility="{Binding StatusText, Converter={StaticResource NullOrEmptyToVis}}" />

            <Border Style="{StaticResource Card}">
                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource Brush.Muted}"
                           Text="Differences between the two runs are observations only — they are not proof of causation. The generated report presents the A/B rows and the same disclaimer." />
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
